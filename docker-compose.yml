services:
  bridge:
    image: ghcr.io/jmgstudios/discordstoatbridgebot:latest
    container_name: stoat-discord-bridge
    restart: unless-stopped
    stdin_open: true
    tty: true
    working_dir: /config
    command: python /app/bridge.py
    volumes:
      - ~/.config/bridgebot:/config
